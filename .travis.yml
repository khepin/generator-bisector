language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-bisector' ]; then cd .. && eval "mv $currentfolder generator-bisector" && cd generator-bisector; fi

